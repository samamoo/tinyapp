<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success ">
    <div class="w-100 navbar-nav d-flex justify-content-between">
      <div class="navbar-nav d-flex justify-content-between">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <!-- <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button> -->
    <!-- <div class="collapse navbar-collapse" id="navbarNavAltMarkup"> -->
      <!-- <div class="navbar-nav d-flex justify-content-between"> -->
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a></div>
        <!--Login/Logout-->
        <%if (username.email) {%>
        <p class="text-white">Logged in as: <%= username.email %></p>
        <form method="POST" action="/logout" class="navbar-nav d-flex justify-content-between">
          <button type="submit" value="logout" class="btn btn-light text-muted">LOGOUT</button>
        </form>
        <%} else {%>
        <form method="POST" action="/login" class="navbar-nav d-flex justify-content-between">
          <input type="text" placeholder="Username" name="username" class="form-control form-control-sm" style="width: 300px; margin: 0.5em">
        <button type="submit" value="login" class="btn btn-light">LOGIN</button>
        </form>
        <%}%>
    </div>
  </nav>
</header>